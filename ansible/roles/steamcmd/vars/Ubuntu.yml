---
# Ubuntu: steamcmd is packaged in the multiverse component (enabled by the 'common' role).
steamcmd_packages:
  - steamcmd

# Path of the steamcmd executable installed by the package.
steamcmd_binary: /usr/games/steamcmd
